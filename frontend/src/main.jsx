// main.jsx
// Entry point — creates the React root and renders <App />.

import { createRoot } from "react-dom/client";
import App from "./App.jsx";

createRoot(document.getElementById("root")).render(<App />);
